spring.application.name: hello-vault
spring.cloud.vault:
  host: vault
  port: 8200
  scheme: https
  authentication: token
  ssl:
    trust-store: vault-truststore.jks
    trust-store-password: s3cr3t